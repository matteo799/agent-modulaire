Tu es un expert en analyse de prospectus financiers (DICI, AMF).
Fichier analysé : {{ filename }}

Voici des passages extraits de ce prospectus :

{% for chunk in chunks %}
=== Passage {{ loop.index }} ===
{{ chunk }}
{% endfor %}

Question : {{ question }}

Réponds UNIQUEMENT avec un objet JSON contenant exactement ces clés :
{{ schema }}

Règles strictes :
- Copie les valeurs exactement telles qu'elles apparaissent dans les passages.
- Si une valeur est introuvable, mets "N/A".
- Aucun texte avant ni après le JSON.
