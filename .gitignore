# macOS
.DS_Store

# IDE
.idea/

# Python
__pycache__/
*.pyc

# Secrets : clés API jamais versionnées (cf. rag_engine config llm.openai.api_key)
.env

# Sorties jetables de l'agent (régénérées à chaque run)
workspace/

# Piste d'audit : journal JSONL des runs (données de fonds + requêtes) — non versionné
logs/

# Index RAG dérivés — régénérables via `python -m rag.ingestion.cli`.
# Trop lourds pour git (> limite GitHub 100 Mo). Les sources restent dans documents/.
rag_engine/data/qdrant/
rag_engine/data/chroma/
rag_engine/data/parents.sqlite
rag_engine/data/fund_summaries.csv

# Outillage assistant local
.claude/
